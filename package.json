{
  "name": "poker-bankroll-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Gest\u00e3o de Poker e Bankroll V2 - WebApp Responsivo e PWA",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/web && rm -rf dist apps/api/dist && cp -r apps/web/dist dist && cp -r apps/web/dist apps/api/dist",
    "test": "npm run test --workspace=packages/shared",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build:api": "npm run build:api --workspace=apps/api"
  },
  "dependencies": {
    "typescript": "^5.7.3",
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "vite": "^6.1.0",
    "@vitejs/plugin-react": "^4.3.4",
    "vite-plugin-pwa": "^0.21.1"
  }
}