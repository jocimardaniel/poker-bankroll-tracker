{
  "name": "poker-bankroll-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Gest\u00e3o de Poker e Bankroll V2 - WebApp Responsivo e PWA",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npx prisma generate --schema=apps/api/prisma/schema.prisma && node node_modules/typescript/bin/tsc -p packages/shared/tsconfig.json && node build.js && rm -rf apps/api/dist && cp -r dist apps/api/dist",
    "test": "npm run test --workspace=packages/shared",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build:api": "npm run build:api --workspace=apps/api"
  },
  "dependencies": {
    "typescript": "^5.7.3",
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "vite": "^6.1.0",
    "@vitejs/plugin-react": "^4.3.4",
    "vite-plugin-pwa": "^0.21.1",
    "@vercel/node": "^5.1.8"
  },
  "type": "module"
}